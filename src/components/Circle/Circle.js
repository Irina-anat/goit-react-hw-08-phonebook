import css from './Circle.module.css'

export const Circle = () => {
    return (
        <div className={css.circle__wrapper}>
            <div className={css.circle}> </div>   
        </div>
    )
};